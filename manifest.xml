<package>
  <description brief="fugu_joy">

     Teleoperation package for the fugu-c robot. Currently includes 
     a joystick sampler library and a joystick teleoperation node.
     
     The node provides several control modes that can be switched on line.
     Joystick commands are translated to twist, wrench or motor levels
     depending on the current mode, and published on the corresponding topic.

  </description>
  <author>Joan Pau Beltran</author>
  <license>BSD</license>
  <review status="unreviewed" notes=""/>
  <url>http://ros.org/wiki/fugu_teleoperation</url>
  <depend package="roscpp"/>
  <depend package="sensor_msgs"/>
  <depend package="srv_msgs"/>
  <depend package="control_common"/>
  <depend package="albatros_motor_board"/>
  <depend package="std_msgs"/>
  <depend package="auv_control"/>

  <export>
    <cpp cflags="-I${prefix}/include" 
         lflags="-Wl,-rpath,${prefix}/lib -L${prefix}/lib -ljoy_state" />
  </export>

</package>


